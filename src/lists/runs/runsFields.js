const runsFields = ({
  id,
  suppliers_category_id,
  matching_object_id,
  matching_policy,
  matching_update_time,
  recommendation_object_id,
  publish_time,
}) => ({
  id,
  suppliers_category_id,
  matching_object_id,
  matching_policy,
  matching_update_time,
  recommendation_object_id,
  publish_time,
})

export default runsFields
